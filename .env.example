# Backend Environment Variables

# MongoDB connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname

# Firebase Admin SDK credentials
# Option 1: Path to service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# Option 2: Or provide JSON string directly (for cloud deployments)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}

# Server port
PORT=4001

# Optional: MongoDB database name (defaults to "betting")
MONGODB_DB=betting

# Optional: Allow requests from specific frontend origin
# Use "*" for dev, specific origin for production
FRONTEND_ORIGIN=http://localhost:5173
